import React from "react";
import styles from "../styles/modules/navbar.module.scss";

const Navbar: React.FC = () => {
  return <nav className={styles.navbar}>Task Management (to-do-list)</nav>;
};

export default Navbar;
